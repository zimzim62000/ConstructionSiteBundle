{% extends '::base.html.twig' %}

{% block title %}
    {{ 'zimzim_actionitem.index.title'|trans({}, 'ZIMZIMConstructionSite') }}
{% endblock title %}

{% block page %}
    <div class="row">
        <div class="small-10 columns">
            <h1 class="text-center title">
                {{ 'zimzim_actionitem.index.title'|trans({}, 'ZIMZIMConstructionSite') }}
            </h1>
        </div>
        <div class="small-2 columns">
            <a class="tiny button" href="{{ path('zimzim_constructionsite_actionitem_new') }}">
                {{ 'button.create'|trans }}
            </a>
        </div>
    </div>


    {{ grid(grid) }}

{% endblock %}
